webdriver {
  driver = chrome

  capabilities {
    browserName = "chrome"
    timeouts {
      script = 30000
      pageLoad = 300000
      implicit = 0
    }
    "goog:chromeOptions" {
      args = [
        "--start-maximized",
        "--remote-allow-origins=*",
        "--disable-notifications"
      ]
    }
  }
}
